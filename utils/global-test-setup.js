// No need for global setup now as we're managing the browser in page fixtures
async function globalSetup() {
    // Nothing to do here
    console.log('Global setup initialized');
}

export default globalSetup;